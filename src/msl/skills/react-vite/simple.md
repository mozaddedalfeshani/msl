# Skill: {{project_type}} — {{preference}}

## Code Style

- Write TypeScript for all files — avoid `any`.
- Use functional components with hooks.
- Keep components small and focused on one concern.
- Use named exports for all components.

## Project Structure

- Components in `src/components/`, pages/routes in `src/pages/` or `src/routes/`.
- Shared utilities in `src/utils/` or `src/lib/`.

## General

- Prefer CSS Modules or Tailwind CSS for scoped styling.
- Keep third-party dependencies minimal.
- Use `React.StrictMode` in development.
